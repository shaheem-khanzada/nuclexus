MONGODB_URI=mongodb://127.0.0.1/your-database-name
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Contract (frontend + webhook filtering)
NEXT_PUBLIC_CONTRACT_ADDRESS=0x...

# WalletConnect project ID for RainbowKit (https://cloud.walletconnect.com)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id

# Alchemy webhook: signing key from Dashboard → Webhooks → your webhook → Signing key
ALCHEMY_WEBHOOK_SIGNING_KEY=your_signing_key_here
